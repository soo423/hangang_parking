# ───────────────────────────────────────────────
# 웹 프레임워크
# ───────────────────────────────────────────────
fastapi==0.115.12          # FastAPI 웹 프레임워크 본체
uvicorn[standard]==0.34.3  # FastAPI를 실행하는 웹 서버 (standard: 자동 재시작 등 추가 기능 포함)

# ───────────────────────────────────────────────
# 데이터베이스 (ORM)
# ───────────────────────────────────────────────
sqlalchemy==2.0.41         # ORM: 파이썬 클래스로 MySQL 테이블을 다루는 도구
pymysql==1.1.1             # SQLAlchemy가 MySQL에 연결할 때 사용하는 드라이버

# ───────────────────────────────────────────────
# 환경변수 관리
# ───────────────────────────────────────────────
python-dotenv==1.1.0       # .env 파일을 읽어서 os.getenv()로 접근할 수 있게 해줌

# ───────────────────────────────────────────────
# 인증 (비밀번호 + JWT 토큰)
# ───────────────────────────────────────────────
pwdlib[argon2]==0.3.0      # 비밀번호 해싱 라이브러리 (argon2 알고리즘, passlib 대체)
PyJWT==2.13.0              # JWT 토큰 생성 및 검증

# ───────────────────────────────────────────────
# 요청 데이터 검증
# ───────────────────────────────────────────────
email-validator==2.2.0     # Pydantic의 EmailStr 타입 사용 시 필요 (이메일 형식 검증)

# ───────────────────────────────────────────────
# 데이터 수집 및 공휴일 처리
# ───────────────────────────────────────────────
requests==2.32.3           # 서울 열린데이터광장 API 호출에 사용 (HTTP GET 요청)
holidays==0.72             # 한국 공휴일 자동 계산 라이브러리 (대체공휴일 포함)

# ───────────────────────────────────────────────
# ML 모델 저장 및 로드
# ───────────────────────────────────────────────
joblib==1.5.3              # ML 모델을 .pkl 파일로 저장하고 불러오는 도구
scikit-learn==1.9.0        # ML 알고리즘 라이브러리 (RandomForest, LinearRegression 등)

# ───────────────────────────────────────────────
# 미들웨어 및 파일 처리
# ───────────────────────────────────────────────
itsdangerous==2.2.0        # SessionMiddleware가 세션 쿠키를 서명할 때 내부적으로 사용
python-multipart==0.0.20   # 파일 업로드(Form, UploadFile) 처리 시 필요

# ───────────────────────────────────────────────
# ML 모델 학습
# ───────────────────────────────────────────────
scikit-learn==1.9.0          # RandomForestRegressor, LinearRegression
pandas                       # DataFrame 처리
numpy                        # 수치 연산 (matplotlib/seaborn 의존성)
matplotlib                   # EDA 시각화 (히스토그램, 산점도)
seaborn                      # EDA 시각화 (히트맵)

# ───────────────────────────────────────────────
# Jupyter 노트북 실행 (5회차)
# ───────────────────────────────────────────────
jupyter                      # train.ipynb 실행용 (PyCharm 내장 Jupyter 안 쓸 경우)
starlette